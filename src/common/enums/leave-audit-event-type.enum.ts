export enum LeaveAuditEventType {
  REQUEST_CREATED = 'request_created',
  REQUEST_UPDATED = 'request_updated',
  SUBMITTED_FOR_APPROVAL = 'submitted_for_approval',
  APPROVED = 'approved',
  REJECTED = 'rejected',
  CANCELLED = 'cancelled',
}
