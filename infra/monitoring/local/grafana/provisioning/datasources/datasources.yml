apiVersion: 1

datasources:
  - name: Prometheus Local
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true

  - name: Loki Local
    type: loki
    access: proxy
    url: http://loki:3100
    editable: true